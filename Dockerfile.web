FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
# Need devDependencies because ts-node-dev is in devDependencies
RUN npm ci --include=dev

COPY . .

# Your backend likely listens on 4001 (you mentioned before)
EXPOSE 4001

CMD ["npm", "run", "dev"]
